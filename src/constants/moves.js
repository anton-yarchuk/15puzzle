/**
 * Enum of all possible moves on the field.
 *
 * @type {Readonly<{Down: string, Left: string, Right: string, Up: string}>}
 */
module.exports = Object.freeze({
  Up: 'up',
  Down: 'down',
  Right: 'right',
  Left: 'left',
});
